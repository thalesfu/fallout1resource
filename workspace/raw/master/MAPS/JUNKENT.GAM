MAP_GLOBAL_VARS:
//MAP VAR                              NUMBER

done_with_Gizmo         := 0;           // (0) TRUE if the dude has just finished
                                        // the seed to kill Gizmo.
dude_in_jail            := 0;           // (1) TRUE if dude is tossed in jail.
talked_to_scared_peasant        := 0;   // (2) TRUE if dude talked to peasant kicked
                                        // out by the CoC.
dude_picked_jail_door   := 0;           // (3) TRUE if dude messes with the jail.
DocLocker_ptr           := 0;           // (4) Pointer to Morbid's locker, for Katja.
dude_jail_date          := 0;           // (5) Set with game day of dude thrown
                                        //     in jail.