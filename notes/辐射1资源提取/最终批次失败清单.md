# 最终批次失败清单

## 口径

最终索引的 `failures.jsonl` 为 0，表示已生成的提取物和派生物没有 stale、缺失或哈希不一致。全量转换仍有 20 个持续失败，全部是原始 MSG 结构异常：DAT 6 个、松散 `DATA` 14 个。它们没有被猜测修复，也没有被索引失败数掩盖。

最终证据清单：

- master：`batch-msg-20260728T130436486876Z.json`，1,944 成功、6 失败。
- data：`batch-msg-20260728T130439673514Z.json`，635 成功、14 失败。
- INT、FRM、MAP、ACM、MVE 最终复跑均为 0 失败。

## DAT 原始 MSG：6

| 路径 | 原因 |
|---|---|
| `TEXT/ENGLISH/DIALOG/OBJ_DUDE.MSG` | 字段数不完整（2,609） |
| `TEXT/FRENCH/DIALOG/OBJ_DUDE.MSG` | 字段数不完整（2,609） |
| `TEXT/SPANISH/DIALOG/DARL.MSG` | 第 79 行消息编号无效 |
| `TEXT/SPANISH/DIALOG/MORPH.MSG` | 字段数不完整（568） |
| `TEXT/SPANISH/DIALOG/NICOLE.MSG` | 字段数不完整（808） |
| `TEXT/SPANISH/DIALOG/OBJ_DUDE.MSG` | 字段数不完整（2,609） |

## 松散 DATA MSG：14

| 路径 | 原因 |
|---|---|
| `ADYTOWNR.MSG` | 第 41 行闭合符不匹配 |
| `CARDRVR.MSG` | 第 103 行闭合符不匹配 |
| `CARGRD.MSG` | 第 86 行闭合符不匹配 |
| `CHDGUARD.MSG` | 第 68 行闭合符不匹配 |
| `crvndrvr.msg` | 第 253 行闭合符不匹配 |
| `ELEVATR.MSG` | 第 1 行消息编号 `#100` 无效 |
| `FISHRSON.MSG` | 字段数不完整（305） |
| `HCHDGRD.MSG` | 字段数不完整（478） |
| `keri.msg` | 字段数不完整（761） |
| `LORENZO.MSG` | 字段数不完整（973） |
| `OBJ_DUDE.msg` | 字段数不完整（3,923） |
| `Richie.msg` | 字段数不完整（172） |
| `RUFUS.MSG` | 字段数不完整（55） |
| `THORNDYK.MSG` | 第 29 行消息编号为空 |

以上均为**已验证的源结构异常**。若未来要修复，应在独立修复层生成补丁副本，不能改写 DAT 提取物或游戏目录中的松散源文件。
