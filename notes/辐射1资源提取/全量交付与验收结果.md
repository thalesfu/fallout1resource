# 全量交付与验收结果

## 自动化全量结论

已完成 inventory 范围内所有受支持格式的全量批处理，并对未支持格式完成原样保留或明确分类。最终统一索引：

- 26,111 条源资源；17 份提取清单；25,243 条已提取资源。
- 16,612 条转换元数据；205,128 个派生文件。
- 索引 stale 提取 0、stale 转换 0、索引失败 0；另有 20 个 MSG 源结构失败，见 [[最终批次失败清单]]。
- 765 个来源冲突路径、1,530 个候选来源全部保留。
- 批次 ID：`sha256:56c470613f91857a68ba9bb57dc5ef7e6002b5c67889e1888ae61d934cc5dd93`。

## 源目录与仓库验收

两个 DAT 与 6 个关键游戏/补丁文件的 SHA-256 全部匹配操作前基线。排除存档后，松散 `DATA/` 仍为 868 个文件、86,279,194 字节；与 inventory 比较缺失 0、新增 0、大小差异 0。最终“路径 + 大小 + mtime_ns + 文件 SHA-256”组合指纹为 `FD928E2DDDD30336425823E7C06BD50B29CB956CC176E6E52FAA13D8C484C52A`。

Git 当前只跟踪 60 个约 500 KB 的工程文件；没有 DAT、游戏资源、PNG、WAV 或 MP4。`workspace/` 仅有说明文件进入 Git，其余清单、原始提取物、派生物和归档均被忽略。

最终工作区主要目录合计 230,526 个文件、3,784,666,516 字节：`raw` 918,257,056 字节，`output` 2,604,712,611 字节，`index` 196,003,520 字节，`manifests` 55,592,114 字节，`archive` 10,101,215 字节。

## 尚未封闭的项目

自动化全量处理已经完成。20 个 MSG 源结构异常已逐项保留，属于已知失败而非待重跑项。唯一未完成的是 [[全量人工抽查表]] 中的主观试听和连续视频观看；它不影响源文件安全、批次完整性或索引正确性，但在完成前不能宣称“全部主观抽查通过”。
