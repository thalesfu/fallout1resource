# INT 全量批次结果

日期：2026-07-28

## 已验证结论

使用 `fallout1resource 0.4.0` 完成 DAT 与松散 `DATA/` 中全部非存档 INT 的可恢复反汇编。DAT 只读提取 952 个，松散 DATA 直接只读处理 27 个；979 个全部成功，无结构失败。

| 项目 | 结果 |
|---|---:|
| 选择 / 成功 / 失败 | 979 / 979 / 0 |
| 源容量 / 派生写入 | 3,709,168 / 255,449,780 字节 |
| 首轮耗时 | 17.408619 秒 |
| 断点复跑：校验后跳过 / 失败 | 979 / 0 |
| 断点复跑耗时 | 6.329299 秒 |
| 过程 / 指令 | 18,319 / 803,755 |
| 消息引用 / 已链接 | 22,339 / 17,615 |

提取清单为 `workspace/manifests/extraction-20260728T125602965501Z.json`；首轮和复跑清单分别为 `batch-int-20260728T130029368054Z.json`、`batch-int-20260728T130047170209Z.json`。

## 消息关联策略

按“松散 DATA 同名 MSG → 同来源 → DAT 英文 MSG”顺序选择唯一候选。576 个脚本获得消息来源；172 个含消息调用的脚本没有同名 MSG，10 个候选 MSG 因原始结构异常无法加载。这 182 个脚本仍独立完成反汇编，并在批次清单中记录警告，没有猜测关联其他文件。

## 未知操作码

895 个脚本包含当前尚未命名的操作码。解析器保留操作码数值、偏移和可确定参数，不把未知指令猜成其他语义；因此它们属于“成功但需后续语义研究”，不是转换失败。

## 恢复与审计

每个脚本输出 JSON、`.disasm.txt`、`.messages.csv` 和 JSON 校验文件。JSON 记录反汇编与消息 CSV 的相对路径、大小和 SHA-256；断点复跑同时核对 INT 源哈希、所关联 MSG 哈希、组件版本和全部派生哈希后才跳过。
