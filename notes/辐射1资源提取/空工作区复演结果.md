# 空工作区复演结果

日期：2026-07-27

复演目录：`fallout1resource/workspace/replay-20260727/`

## 结果

从不存在的工作区开始，依次完成 inventory、提取 dry-run/执行、九项格式转换 dry-run/执行、统一索引 dry-run/执行和重复构建。全过程未复用原工作区的 raw、output、manifests 或 index。

| 项目 | 结果 |
|---|---:|
| inventory 资源 / 冲突 / 不安全路径 | 26,111 / 765 / 0 |
| dry-run 与实际提取 | 4,338 / 4,338 |
| 转换元数据 / 派生文件 | 9 / 99 |
| stale extraction / conversion / derived | 0 / 0 / 0 |
| failures | 0 |

转换覆盖两份 MSG（DAT 英文与松散中文）、HAROLD.INT、HARLDNG/MADDOGAA 两种 FRM 方向模型、HUBOLDTN MAP/PRO/LST 链路、两份 ACM 和 BOIL3.MVE。索引连续两次构建一致，批次 ID 为 `sha256:5931ed3d2c8dd751fbb83c7017a533244b8137fc4e62db7a40b5f223c444ce41`。

## 源目录复核

复演结束后核心文件保持固定基线：

- `MASTER.DAT`：339,379,746 字节，SHA-256 `A79090E035E33C178AEE23FE72F7EF0A5F3BE76F733C252522983AAD22F87364`
- `CRITTER.DAT`：157,085,138 字节，SHA-256 `66800803FC06030573561B4D25ED7A9F0EA7A4B84B734E6D18A9FEFAEEB990A9`

两者时间戳仍为 2026-05-24 15:16:28。Git 工作树干净，`workspace/` 和 `.venv/` 均处于忽略状态。

## 本地容量

复演目录含 4,447 个文件、约 42.73 MiB。当前整个代码工程约 159.13 MiB，包含原工作区、复演工作区、本地 `.venv`，以及 `workspace/cache/ruff-0.15.22/` 中约 43 MiB 的可恢复下载缓存；这些内容均不进入 Git。安全策略阻止了本次自动清理该缓存，可在不再需要离线安装 Ruff 时手工删除。
