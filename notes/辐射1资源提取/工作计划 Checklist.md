# 《辐射 1》资源提取工作计划

关联设计：[[资源提取设计方案]]

工程入口：[[项目导航]] · 操作手册：[[安全运行手册]] · 经验与待决策：[[经验总结与待决策]]

## 0. 范围与安全基线

- [x] 确认代码工程位置：`C:\Users\Thales\codes\github.com\thalesfu\fallout1resource`
- [x] 确认 Obsidian 只保存方案、计划和研究结论
- [x] 记录 `MASTER.DAT` 与 `CRITTER.DAT` 的大小及 SHA-256
- [x] 记录游戏版本、Steam 构建、高清补丁和汉化证据；汉化包正式名称/版本仍待人工确认
- [x] 生成游戏安装目录的只读基线清单，明确排除 `DATA/SAVEGAME/`
- [x] 约定任何写操作只发生在 `fallout1resource/workspace/`

**关卡 A：** 源目录无写入，基线清单可用于操作后复核。

## 1. 初始化代码工程

- [x] 创建 `README.md`、`AGENTS.md` 和开发文档
- [x] 采用 MIT License，并加入 `LICENSE` 和项目元数据
- [x] 建立 `src/`、`tests/`、`config/`、`docs/` 与 `workspace/` 目录
- [x] 编写 `.gitignore`，排除整个 `workspace/`、DAT、提取物、第三方二进制和日志
- [x] 选择 Python 3.11+ 与标准库方案；当前验证环境为 Python 3.12.13
- [x] 配置 Ruff 0.15.22 格式化/静态检查和统一 PowerShell 测试命令
- [x] 完成首次本地 Git 提交：`00bf37f feat(inventory): add read-only DAT1 inventory`

**关卡 B：** 克隆仓库后无需游戏资源也能运行帮助命令和测试。

## 2. 调研格式与工具

- [x] 核对 Fallout 1 DAT1 的目录表、压缩和路径规则
- [x] 区分 Fallout 1 DAT1 与 Fallout 2 DAT2，加入错误格式测试
- [ ] 评估候选解包器的来源、许可证、版本、哈希和单文件提取能力
- [x] 核对 MSG 三字段、字段长度、换行和重复编号语义
- [x] 核对 INT 启动区、过程表、字符串空间和操作码参数规则
- [x] 调研 MAP、PRO、LST、FRM、PAL、ACM、MVE 的可靠解析方案
- [x] 首个里程碑不运行第三方解包器，使用自有只读目录解析器
- [x] 将当前结论写入 `docs/format-notes.md` 和 `docs/tool-audit.md`

**关卡 C：** 只采用来源清楚、可重复调用且输出目录可控的工具。

## 3. “只列清单”里程碑

- [x] 实现 `inventory` 命令，只读取 DAT 目录而不解包内容
- [x] 扫描 `DATA/` 松散资源并排除存档
- [x] 输出来源、`internal_path`、`size`、`type` 等字段
- [x] 为两个 DAT 容器和 JSON 清单计算 SHA-256
- [x] 生成 DAT 与松散文件的重名和来源分层报告
- [x] 抽查目录数量、根目录表示、路径大小写和特殊字符

**关卡 D：** 能重复生成一致的清单，且两个核心 DAT 哈希未变化。

## 4. 安全提取框架

- [x] 实现默认 `dry-run` 和显式输出目录
- [x] 拒绝绝对路径、盘符路径、`..`、Windows 保留名与工作区外目标
- [x] 支持按精确路径、扩展名和资源类别提取
- [x] 同名文件按来源隔离，默认不覆盖现有文件
- [x] 每个原始提取文件记录来源、偏移、存储大小、解压大小和 SHA-256
- [x] 覆盖路径越界、源档案变化、损坏档案、大小异常和覆盖拒绝测试
- [x] 补充同一档案内大小写冲突和多文件批次中断恢复测试

**关卡 E：** 恶意或异常路径不能写出 `workspace/`，失败不会损坏已有产物。

## 5. 小样本端到端验证

- [x] MSG：记录原编码检测，保留全部消息编号出现项，导出 UTF-8 JSON/CSV
- [x] 完成 MSG 里程碑本地提交：`a8b06c2 feat(msg): add safe text conversion`
- [x] INT：生成结构化反汇编结果，并关联同名 MSG
- [x] 完成 INT 里程碑本地提交：`678fddc feat(int): add safe script disassembly`
- [x] FRM/PAL：输出 PNG，同时保留方向、帧、偏移和调色板信息
- [x] 使用 `HARLDNG.FRM` 验证共享方向，并用 `MADDOGAA.FRM` 验证六个独立方向
- [x] 完成 FRM/PAL 里程碑本地提交：`ba40325 feat(frm): add safe palette PNG export`
- [x] MAP/PRO/LST：输出结构化数据，保持列表顺序和编号关系
- [x] 使用 `HUBOLDTN.MAP` 验证地图—对象—PID—LST—PRO—脚本链路
- [x] 完成 MAP/PRO/LST 里程碑本地提交：`d21dce6 feat(map): link maps to prototypes and lists`
- [x] ACM：无损解码并记录采样率与声道
- [x] 使用 `IB1P1XX1.ACM` 验证极小单声道音效，使用 `HROLD1.ACM` 验证哈罗德双声道语音
- [x] 导出 16 位 PCM WAV、结构化 JSON 和校验文件；记录不完整声道帧的静音补样本
- [x] 以 adecode v1.0.0 固定提交核对算法并保留 MIT 许可声明
- [x] 完成 ACM 里程碑本地提交：`5817cb2 feat(acm): add safe WAV decoding`
- [x] MVE：验证可预览转换并记录帧率
- [x] 使用 `BOIL3.MVE` 验证 432×320、150 帧、`125000/8341` fps 与双声道 DPCM 音轨
- [x] 导出原生结构 JSON、全部段 CSV、首帧 PNG、PCM WAV 和 MPEG-4/AAC 预览 MP4
- [x] 固定 FFmpeg/BtbN 构建版本、发布包 SHA-256、EXE、ffprobe 与运行库清单哈希
- [x] 完成 MVE 里程碑本地提交：`738f08e feat(mve): add audited preview conversion`
- [x] 使用 `HAROLD.INT`/`HAROLD.MSG` 验证脚本—文本关联

**关卡 F：** 每类至少一个样本可从派生物追溯到唯一原始资源。

## 6. 全量处理与交付

本节的“全量”指本机 inventory 中除 `DATA/SAVEGAME/` 外的 DAT 与松散 `DATA/` 资源。已实现解析器的格式需要批量转换；尚无解析器的格式至少要完成来源归类、原始提取或不处理原因记录。第 5 节的小样本成功不能代替这里的全量完成状态。

当前 inventory 基线：26,111 个资源，约 958.00 MiB；其中 `MASTER.DAT` 19,784 个、`CRITTER.DAT` 5,459 个、松散 `DATA/` 868 个。以下数量均以此清单为准。

### 6.1 批处理基础设施

- [x] 完成 [[统一资源索引设计草案]]，明确主记录、派生记录、冲突、失败和批次恢复模型
- [x] 从 inventory 生成 `resources.jsonl`、`collisions.csv`、`summary.json` 和组合哈希
- [x] 接入 extraction manifests，补内容哈希、原始文件路径和提取状态
- [x] 接入各格式转换元数据，生成 `derived.jsonl`
- [x] 加入 `failures.jsonl`、stale 检测、批次记录和索引目录恢复语义
- [x] 为 MSG 实现逐文件继续、校验后跳过、显式覆盖恢复和批次清单
- [x] 将相同的可恢复批处理语义扩展到 INT、FRM、MAP、ACM 和 MVE
  - [x] INT：逐文件继续、警告隔离、哈希复跑与显式覆盖
  - [x] FRM/PAL：三来源隔离、全派生物哈希复跑与显式覆盖
  - [x] MAP/PRO/LST：地图及实际依赖重解析、派生物哈希复跑与显式覆盖
  - [x] ACM：DAT/松散来源隔离、末块兼容审计、WAV 哈希复跑与显式覆盖
  - [x] MVE：固定工具链、逐文件继续、全派生物哈希复跑与显式覆盖

### 6.2 全量批次矩阵

- [x] MSG 文本
  - [x] DAT：提取 1,950 个，转换成功 1,944 个，6 个原始结构异常，见 [[MSG 全量批次结果]]
  - [x] 松散 `DATA/`：处理 649 个非存档 MSG，成功 635 个、原始结构异常 14 个
  - [x] 汇总编码置信度、重复编号、瞬时文件占用和最终失败分类
- [x] INT 脚本
  - [x] 提取 DAT 中 952 个 INT，并纳入松散 `DATA/` 的 27 个 INT
  - [x] 979 个全部反汇编成功；按有效来源优先级关联唯一同名 MSG
  - [x] 记录 895 个含未知操作码的脚本、172 个无匹配 MSG 和 10 个 MSG 加载警告，见 [[INT 全量批次结果]]
- [x] FRM/PAL 图像
  - [x] 提取 7,577 个 FRM、2,862 个 FR0–FR5 与 19 个 PAL，并纳入松散覆盖
  - [x] 以原版 `COLOR.PAL` 批量导出 10,439 项、154,276 帧；其他 PAL 保留清单，不猜测替换关系
  - [x] 记录共享方向、分方向家族、帧数、尺寸和异常分类，见 [[FRM PAL 全量批次结果]]
- [x] MAP/PRO/LST 结构
  - [x] 提取 72 个 MAP、4,306 个 PRO、23 个 LST；`SCRIPTS.LST` 包含在 23 个 LST 中
  - [x] 72 个地图全部转换并解析其实际引用的 PRO/LST/脚本关系
  - [x] 记录 132,961 个对象、2,146 个脚本和 9,217 次按地图去重的原型引用；0 个结构失败，见 [[MAP PRO LST 全量批次结果]]
- [x] ACM 音频
  - [x] 提取 DAT 中 2,511 个 ACM，并纳入松散 `DATA/` 的 19 个 ACM
  - [x] 2,530 个全部无损解码 WAV；记录采样率、声道、样本数、时长、位流末块补零与 WAV 尾帧补样本，见 [[ACM 全量批次结果]]
  - [x] 将短音效、语音和长音乐分别完成结构与波形抽样；主观试听见 [[全量人工抽查表]]
- [x] MVE 动画
  - [x] 提取 DAT 中全部 13 个 MVE
  - [x] 全量完成原生容器检查，并用固定 FFmpeg 生成 PNG/WAV/MP4 预览
  - [x] 记录 2 个静音影片、统一 `0x11` 数据格式、分辨率、帧率及显示事件差异；0 个最终失败，见 [[MVE 全量批次结果]]
- [x] 其他 inventory 资源
  - [x] 按扩展名和来源统计 5,131 项当前无转换器的资源
  - [x] 为每类标明“原样提取”“仅索引”或“明确排除”及原因，见 [[其他资源分类与原样提取结果]]
  - [x] 确认 `DATA/SAVEGAME/` 继续排除，不在本轮全量范围内

### 6.3 全量验收与交付

- [x] 每个批次记录选择数量、源容量、输出容量、工具版本、耗时和逐项错误
- [x] 对所有批次执行断点复跑，确认成功项经哈希校验后跳过
- [x] 完成全量后的统一索引、[[最终批次失败清单]] 和重名报告，见 [[全量交付与验收结果]]
- [ ] 建立并完成图片、音频、视频、文本、脚本、地图和原型人工抽查表；结构与目视已完成，尚缺主观试听/连续播放，见 [[全量人工抽查表]]
- [x] 明确松散 `DATA/` 运行时优先于 DAT，同时在研究索引保留所有候选来源
- [x] 全量结束后核验游戏安装目录关键文件哈希，并为 868 个松散文件建立路径/大小/时间戳/哈希组合指纹
- [x] 全量结束后确认 Git 中没有 DAT、私有游戏资源或大体积生成物
- [x] 将各批结果、稳定结论和最终索引摘要整理回 Obsidian
- [x] 所有结论标明“已验证”“工具推断”或“待人工确认”

## 7. 工程知识维护

- [x] 建立 [[项目导航]]，集中链接设计、阶段结果和固定基线
- [x] 建立 [[安全运行手册]]，固化 dry-run、执行、验证和停止条件
- [x] 建立 [[经验总结与待决策]]，汇总跨格式陷阱和 P0/P1/P2 事项
- [x] 将下一阶段收敛为统一索引最小闭环，不直接开始全量解包

## 完成定义

- [x] 在独立空工作区完成 inventory、提取、九项转换和索引复演，见 [[空工作区复演结果]]
- [x] 6.2 中所有纳入范围的全量批次完成，未支持或排除项均有明确记录
- [x] 源游戏目录关键文件哈希不变；松散 DATA 路径与大小和 inventory 零差异
- [x] 所有成功输出具有来源、工具版本和哈希；20 个 MSG 源结构失败均有逐项记录
- [ ] 自动测试、断点复跑和全量代表性人工抽查全部通过
- [x] 最终统一索引无 stale 项，批次 ID 由完整输入确定且有自动化复现测试
- [x] 代码仓库最终只包含程序、测试、配置示例和文档
