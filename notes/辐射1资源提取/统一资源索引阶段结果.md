# 统一资源索引阶段结果

日期：2026-07-27

## 结论

统一索引最小闭环已完成。索引器只读取既有 inventory、提取清单、转换元数据和本地派生文件，不重新读取 DAT 载荷，也不修改游戏目录。

## 实测规模

| 项目 | 数量 |
|---|---:|
| 来源资源 | 26,111 |
| 提取清单 / 结果 | 10 / 4,326 |
| 合并后已提取资源 | 4,324 |
| 转换元数据 / 派生文件 | 9 / 99 |
| 不区分大小写冲突路径 / 候选 | 765 / 1,530 |
| stale / failures | 0 / 0 |

输出位于代码工程的 `workspace/index/`，包括 `resources.jsonl`、`derived.jsonl`、`failures.jsonl`、`collisions.csv`、`summary.json` 和 `index.json.sha256`。

## 恢复与重复执行

0.2.0 批次 ID 为 `sha256:fbe4a2c2a8e8021a90b14c5966e054713999bcc2ab6cc5d8b1abfe385855f9dd`，由输入文件哈希、索引 schema 和程序版本计算。连续两次重建的组合校验清单一致。写入先在临时目录完成，再整体切换正式索引；存在未受管理文件时拒绝覆盖。

当前测试基线为 128 项通过、7 项因 Windows 符号链接权限跳过；Ruff 格式与静态检查通过。来源冲突仍保持待人工审核，没有自动选择松散 `DATA/` 或 DAT 为有效来源。

## 0.3.0 MSG 全量批次更新

完成 [[MSG 全量批次结果]] 后，索引规模更新为 11 份提取清单、6,272 条提取结果、1,949 份转换元数据和 5,919 个派生文件；stale 与索引 failures 仍为 0。新批次 ID 为 `sha256:b9bc08d6a9934e4f4d6089c7307bf1ef1a2f001b20b72427f84752e8d8cfbf64`，干运行与执行结果一致。MSG 自身的 9 个原始格式异常记录在转换批次清单中，不会伪装成成功转换，也不会进入转换元数据。
