# 首次资源清单结果

关联：[[资源提取设计方案]] · [[工作计划 Checklist]]

## 执行范围

2026-07-26 完成首个“只列清单、不解包”里程碑。程序只读访问 Steam 安装目录中的 `MASTER.DAT`、`CRITTER.DAT` 和 `DATA/`；明确跳过 `DATA/SAVEGAME/`。清单写入 `fallout1resource/workspace/manifests/`，没有复制或解包游戏资源。

代码已保存为本地提交 `00bf37f feat(inventory): add read-only DAT1 inventory`，尚未推送到 GitHub。

## 结果摘要

| 来源            | 目录数 |    条目数 | SHA-256                                                            |
| ------------- | --: | -----: | ------------------------------------------------------------------ |
| `MASTER.DAT`  |  65 | 19,784 | `A79090E035E33C178AEE23FE72F7EF0A5F3BE76F733C252522983AAD22F87364` |
| `CRITTER.DAT` |   1 |  5,459 | `66800803FC06030573561B4D25ED7A9F0EA7A4B84B734E6D18A9FEFAEEB990A9` |
| 松散 `DATA/`    |   — |    868 | 本轮未逐文件计算                                                           |

合计 26,111 条：图像 7,577、原型 4,306、音频 2,530、文本 2,599、脚本 979、地图 72、调色板 19、索引 23、视频 13、其他 7,993。

## 校验结论

- 两个 DAT 的 SHA-256 与设计方案中的操作前基线完全一致。
- 发现 765 个不区分大小写的重名路径；多数是 `DATA/` 中的汉化或高分辨率补丁覆盖 DAT 内资源，清单按来源分别保留，没有互相覆盖。
- 最终危险路径计数为 0。第一次扫描曾把 DAT 中合法的 `.` 根目录误报为危险路径；修正规范化逻辑并增加回归测试后重新生成清单。
- JSON 清单约 9.6 MiB，另有 CSV 和 JSON SHA-256 文件；三者均被 `.gitignore` 排除。

下一步进入“安全提取框架”，先实现 `dry-run`、单文件选择、路径越界拒绝和默认不覆盖，再考虑 LZSS 解压。
