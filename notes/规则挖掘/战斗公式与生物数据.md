# 战斗公式与生物数据

日期：2026-09-18。承接 [技能与Perk](技能与Perk.md)。

攻略产出：Vault `07 游戏/辐射1/战斗/战斗公式.md`、`特性/`（16 篇 + 总览）、`生物/`（15 个族群策略 + 219 篇原型页 + 总览，另有 93 篇已有人物/生物笔记被追加“战斗资料”块）。

## 数据来源与可复现命令

```bash
# 技能/Perk/特性表（含 traits）
python3 scripts/extract_skill_perk_rules.py --ce-root ~/codes/github.com/alexbatalov/fallout1-ce \
  --text-root workspace/output/text --output-dir workspace/output/rules
# 战斗静态表：部位惩罚、暴击效果表、大失败表、伤害标志
python3 scripts/extract_combat_tables.py --ce-root ~/codes/github.com/alexbatalov/fallout1-ce \
  --output workspace/output/rules/combat-tables.json
# 遭遇脚本装备候选
python3 scripts/scan_encounter_loadouts.py --scripts-root workspace/output/scripts \
  --output workspace/output/rules/encounter-loadouts.csv
# 生物总目录（原型 + AI + 地图实例 + 已有笔记匹配）
python3 scripts/build_critter_catalog.py --workspace workspace \
  --vault-game-root "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/Thales/07 游戏/辐射1" \
  --output workspace/output/critters/catalog.json
# 生成 Vault 笔记（不加 --apply 只报告）
python3 scripts/generate_critter_docs.py --catalog workspace/output/critters/catalog.json \
  --combat-tables workspace/output/rules/combat-tables.json \
  --loadouts workspace/output/rules/encounter-loadouts.csv \
  --vault-game-root "$HOME/..../07 游戏/辐射1" --apply
```

## 战斗公式要点（已写进 Vault）

- 命中率按固定顺序累加，上限 95%、**无下限**；战斗难度只影响 NPC 打玩家。
- 距离修正：`距离 − 2×感知`（长程武器 4×感知），超出每格 −4%，以内每格 +4%（上限 +8×感知）。
- **省下的 AP 在别人回合会全额加进自己的 AC**（`stat_level`）。
- 暴击率基础值 = 幸运；瞄准部位的暴击加成等于命中惩罚的绝对值，**连发不吃这个加成**。
- **游戏内时间不满 1 天时，掷骰式大成功/大失败完全不触发**（`roll.cc`），而游戏从 12-05 07:21 开始。
- 伤害逐发结算：`(武器伤害 + 远程加成) × 暴击倍率 × 难度 − DT`，再扣 DR%。
- **F1 不读弹药的 AC/DR 修正与伤害倍率**（字段只在 proto 读写处出现），弹种只决定口径。
- 派生属性：HP `15+ST+2×EN`、AP `AG÷2+5`、AC `AG`、序列 `2×PE`、暴击率 `LK`、治疗 `max(EN÷3,1)`。
- AI 逃跑用**绝对生命值** `min_hp`，不是百分比；换目标按 `max(近战伤害, 武器最大伤害) + AC` 排序。

## 脚本端 Perk（本轮解出）

- **意志力**：`REVULSE.INT` 每段造成 `随机(1, 感知)` 点无视护甲伤害并逐段累加，队友按各自感知同样受伤；有 Perk 则整段跳过。**感知越高伤害越高**。
- **动物之友**：辐射蝎与老鼠脚本在 `obj_can_see_obj(自己, 玩家)` 后判断，有 Perk 就不发起攻击。
- **野外专家**：遭遇脚本把等级存进计数器，掷到敌对遭遇类型（1/2/3/4/7）时把类型改成 0 并把计数器减 1，即每级取消一次敌对遭遇。
- **警觉力**：18 个角色脚本在 `description_p_proc` 里做 `do_check(玩家, 感知, 有无此 Perk)`，通过后显示额外描述文本。
- **搜寻者**：`RNDDESRT` 中有 Perk 时 `create_object_sid(34)`（.223 FMJ）并放进物品栏。

## 生物数据

`build_critter_catalog.py` 汇总 312 个生物原型：

- 原型 PRO 的 35 项基础/加成属性、18 项技能点（换算成实际技能值）、kill_type、体型、经验、AI 包、脚本。
- `DATA/AI.TXT` 的 95 个 AI 包参数（逃跑血线、最低命中、侵略性、瞄准频率、脱战距离）。
- 72 张地图里的 1095 个实例：坐标、对象 ID、生命、队伍、脚本、随身物品（按 `flags` 位区分右手/左手/穿戴）。
- 与 Vault 已有笔记的对应：按 front matter 的 `prototype_id` + `map` + `map_object_id` 匹配，794 个实例已有专属人物页。

分布：男性人类 170、女性 79、超级变种人 18、尸鬼 13、老鼠 8、犬类/机器人各 4、死亡爪 3，其余 1–2。85 个原型不出现在任何地图里，由随机遭遇或剧情脚本生成。

## 踩坑

- catalog 里的 `kill_type` 存的是数字 id，而暴击表和族群表按名字索引，两边要显式换算。
- 地图 JSON 的库存条目键名是 `item`，不是 `object`。
- **生成器扫描“已有笔记”时必须排除自己上一轮的产物**，否则第二次运行会把原型页当成已有笔记，往自己身上再追加一遍战斗资料块。已用文件名的“（原型 N）”后缀排除。
- Vault 里的物品笔记同样带 `prototype_id` 字段，匹配生物时要限定在 `人物/` 与 `生物/` 目录下。
- `create_object_sid` 的 PID 是最先压栈的参数，在反汇编里要取调用前的**倒数第 4 个**字面量，取最近的会拿到 tile 或 elevation。

## 待确认（来自源码阅读，写进攻略时已标注）

- Better Criticals 下限在 CE 被改成 −60 对齐 F2，原版 v1.1 可能是 0，即暴力战士的 −30 或许不生效。
- 单发脱靶的误伤逻辑在代码上看似永远不触发（连发误伤确定生效）。
- PID `0x01000008` 在战斗代码里被特判为不可暴击、不计死亡，具体指哪个实体不明。
- 儿童暴击表里有几条永远不会触发的追加效果（属性检定为 −1 却填了标志），应是原版遗留数据。

## 待挖

- 武器与护甲的完整数值表（PRO_ITEM）尚未做成攻略页，可按同样方式生成 `武器防具/`。
- 随机遭遇表本身（`worldmap.cc` 硬编码）：遭遇类型编号、出现地区、特殊遭遇 6 种的触发条件。
- 商人库存与补货逻辑、时间限制（水芯片 150 天、变种人入侵）的具体实现。
- 现身、八面玲珑在约 270 个脚本里的通用反应计算。
